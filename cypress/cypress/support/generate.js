// import {build, fake} from 'test-data-bot';

// const buildUser =  build('User').fields({
// name: fake(f => f.internet.name()),
// phoneNumber: fake(f => f.internet.phoneNumber()),
// email: fake(f => f.internet.email()),
// password: fake(f => f.name.password()),
// })

// export { buildUser };